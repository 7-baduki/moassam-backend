spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

jwt:
  secret: ${JWT_SECRET}

app:
  cors:
    allowed-origins: http://localhost:3000
  oauth:
    success-redirect-uri: http://localhost:3000/oauth/callback
